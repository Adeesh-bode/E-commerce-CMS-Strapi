import { MenuItem } from '@strapi/helper-plugin';
interface UniqueRouteProps extends Pick<MenuItem, 'to' | 'exact'>, Required<Pick<MenuItem, 'Component'>> {
    key: string;
}
export declare function makeUniqueRoutes(routes: UniqueRouteProps[]): UniqueRouteProps[];
export {};
